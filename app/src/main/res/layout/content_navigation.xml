<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:showIn="@layout/app_bar_navigation"
        tools:context=".Navigation"
        android:id="@+id/layout_1">

    <Button
            android:id="@+id/btnPersona"
            android:text="Persona"
            android:textColor="#fff"
            android:textSize="20sp"
            android:layout_width="270dp"
            android:background="@drawable/bg"
            android:layout_height="70dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="40dp" app:layout_constraintTop_toTopOf="parent"
            android:onClick="persona"/>

    <Button
            android:id="@+id/btnEmpleado"
            android:text="Empleado"
            android:textColor="#fff"
            android:textSize="20sp"
            android:layout_width="270dp"
            android:background="@drawable/bg"
            android:layout_height="70dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/btnPersona"
            android:layout_marginTop="40dp"
            android:onClick="empleados"/>

    <Button
            android:id="@+id/btnGastEmpleado"
            android:text="Gastos por empleado"
            android:textColor="#fff"
            android:textSize="20sp"
            android:layout_width="270dp"
            android:background="@drawable/bg"
            android:layout_height="70dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/btnEmpleado"
            android:layout_marginTop="40dp"
            android:onClick="GastoPorEmpleado"/>

</android.support.constraint.ConstraintLayout>